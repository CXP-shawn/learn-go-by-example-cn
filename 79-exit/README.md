# 第 79 关：退出（师兄带你学 Go）

## 🎯 这一关你会学到

错误返回 + main 统一打印 + os.Exit

## 🤔 先想一个问题

的模式替代到处乱 Exit

## 📖 看代码

```go
// 使用 `os.Exit` 可以立即以给定的状态退出程序。

package main

import (
	"fmt"
	"os"
)

func main() {

	// 当使用 `os.Exit` 时 `defer` 将_不会_ 被执行，
	// 所以这里的 `fmt.Println` 将永远不会被调用。
	defer fmt.Println("!")

	// 退出并且退出状态为 3。
	os.Exit(3)
}

// 注意，不像例如 C 语言，Go 不使用在 `main` 中返回一个整数来指明退出状态。
// 如果你想以非零状态退出，那么你就要使用 `os.Exit`。
```

## 🔍 师兄给你逐行拆



## 🏃 跑一下试试



## 💡 师兄的碎碎念



## 🎓 知识点清单

- [ ] 能用 `os.Exit(0)` 和 `os.Exit(1)` 让程序以指定状态码退出
- [ ] 理解 `os.Exit` 不会触发任何 `defer` 语句，资源清理会被跳过
- [ ] 知道 Go 的 `main` 函数无法像 C 那样 `return 1` 来传递退出码
- [ ] 记住退出码惯例：0 表示成功，非零表示失败
- [ ] 能在实际项目中用

## ➡️ 下一关

🎉 恭喜，你已通关 Go by Example 全部课程！回到 [项目首页](../README.md)
